import * as httpStatusCodes from './http-status-codes.constants';
import * as cors from './cors.constants';
import * as envs from './envs.constants';

export default {
  httpStatusCodes,
  cors,
  envs,
};
