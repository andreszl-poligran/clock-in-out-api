{
  "scripts": {
    "start": "npx sls offline --stage dev",
    "deploy": "npx sls deploy --stage dev",
    "deploy:prod": "npx sls deploy --stage prod",
    "deploy-function": "npx sls deploy function -f functionName --stage dev",
    "test": "npx jest --no-cache",
    "test:watch": "npx jest --watchAll --no-cache",
    "coverage": "npx jest --coverage --no-cache"
  },
  "devDependencies": {
    "@types/node": "^18.16.1",
    "@typescript-eslint/eslint-plugin": "^5.61.0",
    "@typescript-eslint/parser": "^5.61.0",
    "cross-env": "^7.0.3",
    "eslint": "^8.44.0",
    "serverless-dotenv-plugin": "^6.0.0",
    "serverless-dynamodb-local": "^0.2.40",
    "serverless-offline": "^12.0.4",
    "serverless-plugin-typescript": "^2.1.5",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "aws-sdk": "^2.1599.0",
    "axios": "^1.6.8"
  }
}
